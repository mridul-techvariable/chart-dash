/**
 * @enum BaseColor
 * @description Defines the base application color
 */
export enum BaseColor {
  primary = "#666666",
}

/**
 * @enum TickColor
 * @description Defines the tick color
 */
export enum TickColor {
  primary = "rgba(0, 0, 0, 0.08)",
}

/**
 * @enum Margin
 * @description Defines the margins which will be used accross the visuall data components
 */
export enum Margin {
  top = "2px",
  left = "2px",
  right = "2px",
  bottom = "2px",
}

/**
 * @enum FontSize
 * @description Font size accross the chart components
 */
export enum FontSize {
  sm = "0.8rem",
  md = "1.5rem",
  lg = "2rem",
}
